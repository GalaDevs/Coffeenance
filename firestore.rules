rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes for development (Test Mode)
    // This matches what you selected when creating the database
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
